import test from "node:test";import assert from "node:assert/strict";
import {applySavedSongMix,captureSongMix,linearGainToGldDb} from "../src/live/song-mix.js";
import {createMixerState} from "../src/live/performance-session.js";import {songId,type PreparedSong} from "../src/domain/song.js";
const song:PreparedSong={song:{id:songId("one"),title:"One",artist:"A",vendor:"V",originalKey:"C",originalBpm:120,originalTimeSignature:{numerator:4,denominator:4}},selectedKey:"C",selectedBpm:120,timeSignature:{numerator:4,denominator:4},durationSeconds:4,stems:[{role:"Electric",displayName:"ELECTRIC 1",sourcePath:"a",durationSeconds:4},{role:"Electric",displayName:"ELECTRIC 2",sourcePath:"b",durationSeconds:4},{role:"Keys",sourcePath:"c",durationSeconds:4}],regions:[],cues:[],cacheFingerprint:"x"};
test("saved song mixes recall one named-bus setting across every stem in that bus",()=>{const mixer=createMixerState(song),channels=[...mixer.channels];channels[0]={...channels[0]!,gain:.7,muted:true};channels[1]={...channels[1]!,gain:.7,muted:true};channels[2]={...channels[2]!,gain:.4};channels[channels.length-1]={...channels.at(-1)!,gain:.6};const saved=captureSongMix(song,{masterGain:.8,channels},"now"),recalled=createMixerState(applySavedSongMix(song,saved));assert.deepEqual(recalled.channels.slice(0,3).map(x=>[x.gain,x.muted]),[[.7,true],[.7,true],[.4,false]]);assert.equal(recalled.channels.at(-1)?.gain,.6);assert.equal(recalled.masterGain,.8);});
test("GLD conversion preserves unity and silence",()=>{assert.equal(linearGainToGldDb(1),0);assert.equal(linearGainToGldDb(0),"-inf");});
